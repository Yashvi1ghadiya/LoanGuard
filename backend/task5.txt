TASK 5 CHECKLISTFor both Regression and Classification modelsLegend:  [BOTH] = do for every model    [CLASS] = classification only    [REG] = regression only1. Model EvaluationCalculate: Accuracy, Precision, Recall, F1-score  [CLASS]Calculate: RSS, RMSE, R²  [REG]2. Check Overfitting / UnderfittingCompare Train score vs Test score  [BOTH]Train score much higher than Test score → OverfittingBoth scores low → UnderfittingTrain and Test scores close → Good fit ✅3. Cross-Validation (5-Fold)  OR BootstrapRun 5-fold (or 10-fold) cross-validation on the training data  [BOTH]Note the average score across foldsNote the score spread (high spread = unstable model)4. Compare All ModelsFill in one table below (Classification OR Regression) for every model triedClassification models:ModelAccuracyPrecisionRecallF1-scoreRegression models:ModelRSSRMSER²Pick the model with best score AND stable cross-validation result  [BOTH]5. Hyperparameter TuningUse GridSearchCV or RandomizedSearchCV on your best model  [BOTH]Try different values of key settings (e.g. n_estimators, max_depth, learning_rate)Note the best combination it findsRe-test the tuned model on the test set — confirm score improved  [BOTH]6. Try Advanced ModelsRandom Forest(Bagging)  [BOTH]AdaBoost [BOTH]Gradient Boosting [BOTH] (Optional)